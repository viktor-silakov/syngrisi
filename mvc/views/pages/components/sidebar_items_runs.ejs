<% items.forEach(function(item){ %>
    <% const itemCount = (allTests.filter((tst) => tst.run == item.id)).length %>
    <% if (itemCount > 0){ %>
        <div name="run-menu-item-container"
             class="row pl-1 pb-1 w-100 btn-light border-silver mt-1 mr-0 row justify-content-around1">
            <div class="col-auto pl-2 pr-1">
                <input name="run-item" class="small" type="checkbox" suiteid="<%= item.id %>"
                       id="run-checkbox-<%= item.id %>">
            </div>

            <div class="col-7 pr-0 pl-1 pb- text-truncate">
                <a href="?runName=<%= item.name %>" class="small text-dark"><%= item.name %></a>
            </div>

            <div class="col-3 pr-1 pl-1 pt-0 ml-3 text-truncate silver2 small1 bg-sidebar2 rounded text-right align-self-center">
            <span class="small bg-secondary silver rounded p-1" style="opacity: 70%">
                <%= item.formattedUpdatedDate %>
            </span>
            </div>

            <div class="col-1 pr-1 pl-1 pt-0 text-truncate silver2 small1 bg-sidebar2 rounded text-right align-self-end">
            <span class="small bg-sidebar silver rounded p-1">
                <%= itemCount %>
            </span>
            </div>

        </div>
    <% } %>
<% }); %>
