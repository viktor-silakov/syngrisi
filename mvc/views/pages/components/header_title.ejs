<% if(currentItem) { %>
    <a href="?suitename=<%= currentItem.name %>" class="unlink">
        <span><%= currentItem.name %></span>
    </a>
<% }else{ %>
    <a href="/" class="unlink">
        <span>All tests</span>
    </a>
<% } %>
